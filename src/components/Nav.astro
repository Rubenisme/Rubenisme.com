<nav>
  <ul>
    <li><a href="#home">Home</a></li>
    <li><a href="#about">About</a></li>
    <li><a href="#projects">Projects</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <button id="theme-toggle" aria-label="Switch theme" title="Switch theme">
    <span class="theme-dot"></span>
    <span class="theme-dot"></span>
    <span class="theme-dot"></span>
  </button>
</nav>

<style>
  nav {
    position: fixed;
    top: 1.25rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 100;
    display: flex;
    align-items: center;
    padding: 0.85rem 1.5rem 1.2rem;
    background: rgba(8, 59, 119, 0.6);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    border: 1px solid rgba(72, 202, 228, 0.25);
    border-radius: 999px;
    white-space: nowrap;
  }

  /* Restore visible cursor over the nav pill */
  nav, nav * { cursor: auto !important; }
  nav a, nav button { cursor: pointer !important; }

  ul {
    list-style: none;
    display: flex;
    gap: 1.75rem;
  }

  ul a {
    color: rgba(255, 255, 255, 0.85);
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    transition: color 0.2s;
  }

  ul a:hover {
    color: #48cae4;
  }

  #theme-toggle {
    display: flex;
    align-items: center;
    gap: 5px;
    background: none;
    border: none;
    padding: 4px 4px 4px 1rem;
    margin-left: 0.5rem;
    border-left: 1px solid rgba(72, 202, 228, 0.25);
  }

  .theme-dot {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    border: 1.5px solid rgba(255, 255, 255, 0.2);
    transition: border-color 0.2s, transform 0.15s;
  }

  /* Dot colors = background color of each theme */
  .theme-dot:nth-child(1) { background: #f4f8fb; } /* Tropics */
  .theme-dot:nth-child(2) { background: #0d1f35; } /* Deep */
  .theme-dot:nth-child(3) { background: #080e1a; } /* Midnight */

  .theme-dot.active {
    border-color: #48cae4;
    transform: scale(1.35);
  }

  @media (max-width: 600px) {
    nav { top: 0.75rem; padding: 0.7rem 1rem 1rem; }
    ul  { gap: 1rem; }
    ul a { font-size: 0.82rem; }
    #theme-toggle { padding-left: 0.75rem; }
  }
</style>
