<section id="about">
  <div class="container">
    <h2>About</h2>
    <div class="columns">

      <div class="col">
        <h3>Professional</h3>
        <p>
          I build software — from web apps and automation tools to personal projects
          that scratch an itch. I enjoy the full stack: designing good architecture,
          writing reliable back-end logic, and connecting the two. Whether that's
          wiring an LLM into a decision engine exactly where its reasoning fills the gaps,
          or building business logic that runs in production for large companies — I enjoy
          both ends of that spectrum. I believe in using the right tool for the job,
          though sometimes the fun is in the overkill: training reinforcement learning
          agents (Rainbow DQN, PPO) for problems that probably didn't need them.
        </p>
        <ul class="skills">
          <li>C# / .NET</li>
          <li>Python &amp; RL (PyTorch / SB3)</li>
          <li>Other AI — CSPs &amp; beyond</li>
          <li>JavaScript / TypeScript</li>
          <li>Astro / Svelte / React</li>
          <li>Discrete Mathematics</li>
        </ul>
      </div>

      <div class="col">
        <h3>Personal</h3>
        <p>
          Code is both my work and my hobby — I often build things just because
          they seem fun or useful. When I'm not at a keyboard, I'm drawn to the
          ocean: my wallpapers are almost exclusively aerial shots of Bora Bora,
          the Maldives, and the Caribbean. That palette found its way here too.
        </p>
        <div class="photo">
          <img src="../images/aerial-ocean.jpg" alt="Aerial ocean view" />
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  section {
    padding: 6rem 2rem;
    background: var(--bg-page);
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  h2 {
    font-size: 2rem;
    font-weight: 700;
    color: var(--text-heading);
    margin-bottom: 3rem;
  }

  .columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
  }

  h3 {
    font-size: 0.85rem;
    font-weight: 700;
    color: #48cae4;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
  }

  p {
    line-height: 1.7;
    color: var(--text-primary);
    margin-bottom: 1.5rem;
  }

  .skills {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .skills li {
    background: var(--pill-bg);
    color: var(--pill-text);
    font-size: 0.8rem;
    padding: 0.25rem 0.75rem;
    border-radius: 999px;
  }

  .photo {
    border-radius: 25%;
    overflow: hidden;
    margin: 0 auto;
    aspect-ratio: 1 / 1;
  }

  .photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (max-width: 768px) {
    .columns { grid-template-columns: 1fr; }
  }
</style>
